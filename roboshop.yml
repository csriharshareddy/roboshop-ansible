- name: Roboshop Setup
  hosts: all
  become: true
  roles:
    - name: "Print the current play name"
      ansible.builtin.debug:
        msg: "The currently executing play is: {{ component_name }}"
    - "{{component_name}}"


